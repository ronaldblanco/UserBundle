{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AutenUsers list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <!--<th>Password</th>-->
                <th>Email</th>
                <th>Isactive</th>
                <th>Role</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('auten_users_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.username }}</td>
                <!--<td>{{ entity.password }}</td>-->
                <td>{{ entity.email }}</td>
                <td>{{ entity.isActive }}</td>
                {% for entityrole in entitiesrole %}
                	{% if entityrole.id == entity.role %}
                		<!--<td>{{ entity.role }}</td>-->
                		<td>{{ entityrole.role }}</td>
                	{% endif %}
                {% endfor %}
                <td>
                <ul>
                    <li>
                        <a href="{{ path('auten_users_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('auten_users_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('auten_users_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
