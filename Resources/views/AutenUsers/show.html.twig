{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AutenUsers</h1>

    <table class="record_properties">
        <tbody>
            <!--<tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>-->
            <tr>
                <th>Username</th>
                <td>{{ entity.username }}</td>
            </tr>
            <!--<tr>
                <th>Password</th>
                <td>{{ entity.password }}</td>
            </tr>-->
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{{ entity.isActive }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ entityrole.role }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('auten_users') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('auten_users_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
